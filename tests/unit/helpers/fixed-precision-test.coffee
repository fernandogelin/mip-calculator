`import { fixedPrecision } from '../../../helpers/fixed-precision'`
`import { module, test } from 'qunit'`

module 'FixedPrecisionHelper'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = fixedPrecision 42
  assert.ok result
